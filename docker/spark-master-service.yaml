apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: spark-master
  name: spark-master
  namespace: data
spec:
  type: NodePort
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
    - name: "7077"
      port: 7077
      targetPort: 7077
  selector:
    io.kompose.service: spark-master
